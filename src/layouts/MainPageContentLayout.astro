---
interface Props {
  sectionId: string;
  sectionTitle: string;
  className?: string;
  scrollMarginTop?: string;
}

const { sectionId, className, sectionTitle, scrollMarginTop }: Props =
  Astro.props;
---

<section id={sectionId} class="wrapper">
  <div class:list={["container", className]}>
    <h1 class="section-title">{sectionTitle}</h1>
    <slot />
  </div>
</section>

<style define:vars={{ scrollMarginTop }}>
  .wrapper {
    position: relative;
    width: 100%;
    display: flex;
    padding: 1.5rem 0;
    scroll-margin-top: var(--scrollMarginTop);
  }

  .container {
    margin: 0 auto;
    display: flex;
    flex-direction: column;
    gap: 1rem;
    max-width: var(--content-max-width);
    padding: 0 var(--content-padding-screen-l);

    @media (width < 768px) {
      padding: 0 var(--content-padding-screen-m);
    }
  }
</style>
